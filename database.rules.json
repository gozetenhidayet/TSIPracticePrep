{
  "rules": {
    ".read": false,
    ".write": false,
    "rooms": {
      "$roomCode": {
        "events": {
          ".read": true,
          ".write": true,
          "$eventId": {
            ".validate": "newData.hasChild('ts') && newData.child('ts').isNumber()"
          }
        }
      }
    }
  }
}
