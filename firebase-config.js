/*
 * TSI Practice Prep — Firebase configuration
 *
 * This file is intentionally empty by default. Cross-device account sync
 * (student/teacher accounts, classroom sync) stays disabled and the site
 * falls back to local, device-only storage until you fill this in.
 *
 * To enable real cross-device sync:
 *   1. Create a Firebase project + Web App at https://console.firebase.google.com
 *   2. Enable Authentication (Email/Password + Anonymous) and Firestore.
 *   3. Paste your Web App config below.
 *   4. See FIREBASE-SETUP.md for the full checklist, including production
 *      security rules — do not launch with open/development rules.
 */
window.SCOREPATH_FIREBASE_CONFIG = null;
/* Example, once you have real values:
window.SCOREPATH_FIREBASE_CONFIG = {
  apiKey: "...",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "...",
  appId: "..."
};
*/
