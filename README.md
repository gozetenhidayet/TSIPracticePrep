# ScorePath Practice

Free, original SAT / ACT / TSIA2 practice: skill-tagged questions with full explanations, mistake-pattern analysis (concept gap, process error, reading trap, pacing/guess), pacing tools, a Teacher Classroom for live in-class sessions with roster and error analytics, and an optional student account for cross-device sync.

V20 focused on clearer navigation, a simplified header, direct exam selection, a redesigned Teacher Classroom, and 6-character alphanumeric class codes.

## What this pass fixed and added

This repository came in as a "GitHub ready" export with a few gaps that would have broken it on a real static host. This pass closed those and layered on the pieces needed to run the site professionally, monetize it, and get it indexed:

**Fixed a real bug that broke most of the site.** `index.html` had two `$("heroTimed").onclick=...` calls targeting an element ID that doesn't exist anywhere in the page. Because that line lived inside a large inline `<script>` block, the resulting `TypeError` aborted the rest of that script — silently skipping the `satStore`/`satState`/`actStore`/`actState` declarations and everything after them. That, in turn, is why the SAT/ACT dashboards, mistake history, goal ring, and focus-lock features threw `is not defined` errors at runtime. Fixed with a small null-safe `safeClick()` helper; verified with Playwright that all pages now load with zero console errors.

**Fixed a second, separately-hidden bug in Teacher Classroom.** `teachers.html` read and wrote a `securityEvents` array (for the Secure Timed Mode activity log) that was never declared anywhere. Every call to `renderAll()` — including the one that runs immediately on page load — threw `securityEvents is not defined` partway through `renderRoster()`, which silently skipped `renderOverview()`, `renderQuestions()`, `renderStudentsTab()`, and `renderSkills()` every single time. In practice this meant the Overview, Questions, Students, and Skills analytics tabs never populated, ever — the single biggest feature this platform has over a plain question-answering site was completely non-functional. Fixed by declaring `securityEvents=[]` alongside the other session state. Verified end-to-end with two real browser tabs (a teacher room + a student joining, answering, and finishing): roster sync, live progress, and all four analytics tabs now populate correctly.

**Made a defensive console wrapper (`spConsole`).** Some embedding/preview contexts run pages inside a sandboxed `<iframe>` with a restricted `console` shim that only implements a subset of the real console API. This page called `console.info(...)` in three places (bank-size diagnostics) and `console.error(...)` in one; in an environment where `console.info` isn't a function, that throws and can abort a script block the same way the `heroTimed` bug did. All four calls now go through `spConsole(method, ...)`, which checks the method exists before calling it and silently no-ops instead of throwing if it doesn't. Verified by simulating a restricted console (only `log`/`warn`/`error`, no `info`) in Playwright — zero errors.

**Added real Open Graph / Twitter Card images and an Apple touch icon.** `index.html`, `sat.html`, `act.html`, `tsia2.html`, and `teachers.html` had no `og:image` at all, so a link shared in Slack, iMessage, or Twitter/X would render as a bare title with no preview image, and "Add to Home Screen" on iOS (outside the PWA manifest flow) had no icon to use. Generated `og-image.png` (1200×630, on-brand) and wired up `og:image`, `og:url`, `og:site_name`, `twitter:image`, and `twitter:card=summary_large_image` on all five pages, plus `<link rel="apple-touch-icon">`.

**Added the five files `index.html` and the account/classroom pages already referenced but that weren't in the export:**
- `firebase-config.js` — empty by default (`window.SCOREPATH_FIREBASE_CONFIG = null`); cloud sync stays off until you fill in a real Firebase project config.
- `sync-engine.js` — implements `window.ScorePathSync` (student account sign-in/create/device-only mode). Throws a clear, honest error if you try to sign in before Firebase is configured, rather than pretending to create an account.
- `realtime-adapter.js` — implements `window.ScorePathRealtime` for Teacher Classroom. Defaults to `BroadcastChannel` (same-browser, tab-to-tab local demo — exactly what FIREBASE-SETUP.md already documented as the intended fallback); switches to Firebase Realtime Database automatically once a real config is present.
- `manifest.webmanifest` + `icons/icon-192.png` + `icons/icon-512.png` — real PWA icons (generated, not placeholders) so "Add to Home Screen" works.
- `sw.js` — a conservative service worker: caches only same-origin pages for offline access, never touches third-party requests (Desmos, Firebase, AdSense, fonts), and always fetches fresh over cache.
- `favicon.svg` / `app-icon.svg` — the site linked to both filenames; both now exist with the same brand mark.

**Wired up real, consent-gated Google AdSense.** The CSS already had `.adSlot` / `.adZone` placements hidden by default and only revealed via an `.ads-enabled` class that nothing ever set — the plumbing existed but nothing was connected. Added:
- A cookie-consent banner (bottom of `index.html`) — ads and any related cookies only load after a visitor clicks Accept; declining or ignoring it keeps the visit ad-free. A "Cookie Preferences" link in the footer reopens it anytime.
- `ADSENSE_CLIENT_ID` / `ADSENSE_AD_SLOTS` config placeholders (search `scorepath-adsense-consent` in `index.html`) — empty until you fill them in, so no fake or placeholder ad ever shows to a real visitor.
- `ads.txt` at the repo root.
- Ads still never show during an actual practice session — the existing `.exam-running` rule (unchanged) already guarantees that, and this pass confirmed it's genuinely wired to a live `MutationObserver`, not dead CSS.

**Added `robots.txt` and `sitemap.xml`.** The sitemap lists the real public pages and excludes `teacher-login.html`, `student-account.html`, and `student-room.html` (already marked `noindex,nofollow` in their own `<meta>` tags).

**Built the 8 pages the footer nav already linked to but that didn't exist:** `about.html`, `contact.html`, `disclaimer.html`, `editorial-standards.html`, `graduation-guide.html`, `privacy.html`, `resources.html`, `terms.html`. Content was written to match what `index.html` already promises elsewhere on the site (the Trust/Quality Center section, the Accuracy Standards section, `QUESTION-BANK-QUALITY.md`) rather than inventing new claims. `sat.html`, `act.html`, and `tsia2.html` footers now link to all of these too, plus a shared favicon.

**Made the cookie-consent banner work on every page, not just `index.html`.** Previously only the homepage had a working banner; every other page either had none or a dead footer link. Added `consent-banner.js`, a single self-installing script (injects its own CSS, the banner markup, and the "Cookie Preferences" reopen link into whatever footer a page has, or a small fixed corner link on pages with no footer) that shares the same `localStorage` key as `index.html`'s own banner, so a visitor's choice is honored consistently site-wide. Wired into `sat.html`, `act.html`, `tsia2.html`, `teachers.html`, `teacher-login.html`, `student-account.html`, `student-room.html`, and all 8 info pages. `index.html` keeps its own original inline banner (already tied directly to its AdSense activation code) rather than being swapped over, to avoid ID collisions. Verified with Playwright on a representative page from each family: banner shows on first visit, Accept hides it and persists the choice, and the footer's "Cookie Preferences" link reopens it.

**Added an explicit back button to every page except the homepage.** `sat.html`, `act.html`, `tsia2.html`, and the 8 info pages now show "← Back to ScorePath Practice" in the hero; `teacher-login.html` and `student-account.html` already had one and were left as-is; `student-room.html` (the classroom join screen) had none and now does too. Verified with Playwright across all 15 non-home pages.

**Replaced the placeholder `contact.html` mailto link with a real contact form that never exposes an email address.** The old page displayed `support@scorepathpractice.com` directly in the HTML (copyable and scrapeable) and did nothing but open the visitor's own mail client. The new page is a real form (name, email, topic, message, honeypot spam field) that POSTs via `fetch()` to [Web3Forms](https://web3forms.com), a service built exactly for this: the site owner gets a public "access key" tied to a verified inbox, and that key can only submit messages *to* that inbox — it cannot be used to read it, so nothing sensitive is exposed even though the key sits in view-source. No email address appears anywhere in `contact.html`'s HTML or JS. Until a real key is pasted in, the form shows an honest "not connected yet" message instead of silently failing or pretending to send — see "Turning on the contact form" below for the one-minute setup. Verified with Playwright: submitting with the placeholder key shows the correct error state, and no `support@`-style string appears anywhere in the page source.

**Audited the entire 5,000-question bank and fixed a real generator bug that was silently duplicating questions.** The site claims (in its hero copy, about page, and Trust Center) that the practice bank has 5,000 original questions. To verify that claim, every question in `window.ScorePathBank` was extracted with Playwright and checked programmatically: structural completeness (unique ID, valid answer index, four unique choices, a real explanation, choice-by-choice rationale, a strategy tip — the same rule `QUESTION-BANK-QUALITY.md` already documents), full-text duplication (same question + passage + choices + answer as another item), and, for every math item whose format could be parsed, an independent recomputation of the correct answer from the numbers in the question text. The recomputation (1,122 math items checked) found **zero wrong answer keys** — every math item's marked answer really is correct. But the duplication check found a serious problem: the "5,000-question" generator (`v17SATOne`/`v17ACTOne`/`v17TSIOne`, the code that builds most of the bank) had a bug where a question's internal parameters were selected using `i % 12` (or `% 10` for TSIA2) — the exact same number used to pick *which* question template to use for a given slot. Whenever a parameter's own cycle length divided evenly into that same 12 (or 10), the parameter came out **constant** for every single item generated by that template, instead of varying. In practice this meant things like a circle's radius, a sentence's subject noun, or an example passage's name were frozen to one value for the entire life of a template — so the "5,000 questions" were actually well under 1,000 distinct questions, each copy-pasted under a different ID to hit the count target. The worst single offender ("Which inference is most reasonable?" in the TSIA2 bank) was the literal same question repeated 189 times. Fixed the root cause — parameters now derive from a separate counter (`Math.floor(i / cycle length)`) that is never tied to the template-selection index — corrected one knock-on math bug the fix exposed (a circle radius of 2 made two answer choices coincide), and meaningfully expanded several of the smallest hard-coded vocabulary/example lists that were themselves acting as a bottleneck. Verified before/after with the same script: unique-question share rose from **~16% (808 of 5,179) to ~48% (2,523 of 5,179)** across the three banks, with the worst individual repeat counts cut from 150–189× down to about 20–30×. The bank is measurably more original than it was, though it is not the case that every one of the 5,000 slots is now a unique question — see "Known gaps" below for what full uniqueness would require and why it wasn't attempted in this pass.

## Second pass: bugs found from real screenshots, mobile audit, and a monetization push

The first pass above closed out the initial "GitHub ready" gaps. This second pass responded to real user-reported screenshots and then did a ground-up completeness/mobile/monetization audit, since a site can pass every automated check and still have a core feature that's completely dead — which is exactly what turned up here.

**Fixed duplicate/mislabeled footer links.** The homepage footer had "Teacher Resources" wrongly pointing at `resources.html` instead of `teachers.html`, plus a redundant third link pointing at `resources.html` again. Both fixed; all 13 footer links (3 in-page anchors, 9 file links, the JS-driven Cookie Preferences link) verified individually.

**Fixed unreadable exam-card text on hover.** `.examcard:hover` lightened the card background but never recolored the descendant text/buttons that were styled for the original dark background, so the "Digital SAT" heading, the subtitle, and the ghost "Diagnostic" button became low-contrast against the lightened background on hover. Added hover-state color rules for `h3`, `.sub`, `li`, and the secondary button. Verified with a computed-style check and a screenshot.

**Fixed the "Daily practice is temporarily unavailable" bug — every Daily Practice click, for every visitor, since the original export.** This was the most serious bug found this session, and it predates every earlier pass; nothing had ever click-tested this specific button end-to-end. Two separate scope bugs stacked: (1) `bankFor()`/`adaptiveSet()` read a bare `PRO_BANK` variable that only exists inside one inline `<script>`'s own closure — invisible to the code that calls them — so `adaptiveSet()` always returned an empty set and the very first line of `daily()` always failed; fixed by reading `window.ScorePathBank` instead, the one safe cross-script reference the bank is actually published under. (2) Fixing that surfaced a second error, `v9Launch is not defined`, because `v9Launch`/`v9Pending` are also private to that same closure; fixed by exposing `window.v9Launch` and a `window.v9SetPending()` setter, and pointing `daily()` at those. Verified end-to-end for all three exams with fresh browser contexts over the `file://` protocol (matching the user's own reproduction) — zero dialogs, zero errors, a real practice shell opens with a real question set.

**Verified the full student practice flow end-to-end for the first time this session, on purpose, given the above.** Diagnostic → Start Test → answer every question → Submit → Next/Finish → results screen, for SAT, ACT, and TSIA2, via Playwright driving the actual UI (not calling internal functions directly). All three completed correctly with a populated score, accuracy, and question-review list.

**Found and fixed two real mobile-only bugs that no earlier pass had caught**, because the existing regression suite only checked for page-load horizontal overflow, not layout correctness after real interaction:
- The homepage's SAT/ACT/TSIA2 category nav (`.catnav`) is styled by two different `@media(max-width:870px)` blocks left over from two different design iterations. The newer block (a horizontally-scrollable pill strip) never reset `flex-direction` or `align-items` from the older block (a `flex-direction:column` hamburger-dropdown design it was meant to replace), so on every phone-width screen the three exam pills rendered as a full-width vertical stack that pushed into and partly covered the hero heading, instead of the intended horizontal scrolling row. Fixed by adding `flex-direction:row!important;align-items:flex-start!important` to the active rule. Verified with a real 390px-viewport screenshot before and after.
- During an active practice test on a 390px phone, the in-test toolbar (Reference / Desmos / Scratchpad / Read / Fullscreen / Close) had no overflow handling and no wrap, so once more than a couple of tool buttons were visible for a given question, the row simply extended past the right edge of the screen with nothing to scroll it — meaning **Fullscreen and the "×" close-test button could be completely unreachable** on a phone for some question types, with no way to exit the test from that control. Fixed by making the toolbar horizontally scrollable at that breakpoint (matching the pattern already used for the nav pills). Verified: all six controls now sit within the 390px viewport and the row scrolls to reach any that don't fit at once.

**Cleared the 22 broken `skills/*.html` links instead of leaving them 404ing.** `sat.html`, `act.html`, and `tsia2.html` each linked to individual skill pages that were never built. Rather than ship broken links (bad for users, bad for AdSense review, bad for crawl budget), the skill cards are now plain descriptive cards with no link — same content, nothing to 404. Building these out for real is the single highest-leverage next SEO/monetization project; see "Monetization roadmap" below.

**Fixed a real Search Console inconsistency.** `teachers.html` (a live classroom + student-analytics tool, not a marketing page) correctly carries `<meta name="robots" content="noindex,nofollow">`, but `sitemap.xml` listed it anyway — telling Search Console to index a URL the page itself says not to index, which shows up as a "Submitted URL marked 'noindex'" error in Search Console. Removed it from the sitemap (the correct fix, per Google's own guidance, rather than also blocking it in `robots.txt`, which would stop Googlebot from ever seeing the noindex tag in the first place).

**Added the missing canonical tag on `index.html`.** Every other page already had `<link rel="canonical">`; the homepage had a stale comment instead. Added, using the same `https://scorepathpractice.com/index.html` convention already used in its own `og:url` and in `sitemap.xml`.

**Expanded real ad inventory from 1 page to 7.** Before this pass, only `index.html` had any `.adSlot`/`.adZone` placements — every other page, including `sat.html`, `act.html`, and `tsia2.html` (the pages most likely to receive direct organic search traffic once indexed), had zero ad-eligible inventory even after a visitor accepted the cookie banner. Extended the shared `consent-banner.js` (already loaded site-wide) with the same off-by-default AdSense activation logic `index.html` uses, and added one clearly-labeled, consent-gated `.spAdSlot` placement to `sat.html`, `act.html`, `tsia2.html`, `about.html`, `resources.html`, and `graduation-guide.html` — never on login/account pages, never on legal pages, and never during an active test. Verified the full mechanism end-to-end with a temporary test client ID (reverted before commit): consent accepted → AdSense script loads → `ins.adsbygoogle` unit created with the right client ID → reverts to fully hidden the moment a real ID isn't configured, exactly like `index.html`'s original slots. See "Monetization roadmap" below for why this is the top concrete change from this pass.

## Third pass: exam-page polish, navigator flag fix, flashcards, and Teacher Classroom visual parity

This pass responded to specific feedback on the delivered screens: the exam session should look and feel like a real, professional exam product (not "boring" placeholder styling), flagged questions should be obviously flagged, the question-number buttons were too small, the flashcard feature looked broken, "My Results" needed a professional pass, and the Teacher Classroom needed the same visual polish as the exam pages.

**In-test top banner redesigned.** The sticky testbar during an active test now has a colored gradient accent bar along its bottom edge (purple/blue/green for SAT, orange for ACT, blue for TSIA2 — a different accent per exam so a student always has a quick visual cue for which test they're in), circular icon-style buttons for Reference/Desmos/Scratchpad/Read/Fullscreen, a red-tinted "×" close button so it doesn't get mistaken for a neutral tool, and a tabular-numeral timer so the digits don't shift width as the clock counts down. Verified with a full-viewport screenshot.

**Fixed a real bug: flagged questions were invisible in the question navigator.** A CSS rule that runs only while a test is active force-set every question-number button to the same cream background (`#fff8dc!important`), which buried the subtle box-shadow the `.flagged` style used to indicate a flag — so flagging a question during a real test produced no visible change at all. Rewrote the active-test navigator styling so answered, current, and flagged are each a distinct, visible color, flagged questions additionally get a small amber flag badge in the corner, and flagged+answered gets its own two-tone treatment. Verified via Playwright: flagging two different questions during a live test now shows two clearly distinguishable flag badges.

**Enlarged the question-number buttons.** They were 31px tall with 11px text — genuinely small, exactly as reported. Increased to 40px with 13px text and slightly more grid spacing.

**Fixed the flashcard feature's real problem: not a broken card, an undiscoverable one.** The flip logic itself already worked — clicking the card correctly flipped it — but the only hint that it was interactive ("Tap the card to reveal the answer") got overwritten by the card's own content the instant the modal opened, so a student had no way to know the plain text they were looking at was tappable. Added a persistent "↻ Tap to flip" caption that never gets overwritten, a color-coded PROMPT/ANSWER badge, distinct front/back card colors, and a Prev button alongside the existing Next button.

**"My Results" — verified fully functional, one real polish gap fixed.** Investigated end-to-end: the panel is genuinely data-driven and correctly shows 0 tests / — / — before any test is completed, then populates Tests, Recent avg, Best, and a recent-test row the moment a test finishes — this was already working, not broken. The one real gap: it displayed raw internal mode identifiers verbatim, e.g. "sat-diagnostic" instead of "Diagnostic," or "v9-sat-pt2" instead of "Practice Test 2." Added a `prettyMode()` formatter that turns every internal mode string into a readable label across diagnostics, practice tests, adaptive modules, and full ACT sections.

**Teacher Classroom given the same visual treatment as the exam pages.** Added the matching gradient accent bar under the header, gradient-filled action buttons with hover lift, color-coded left borders on the Joined/Working/Finished/Average/Mode metric cards, a subtle underline accent beneath the class code, and hover states on roster rows, tabs, and analytics cards — so the teacher tool now reads as one consistent professional product with the exam pages instead of two different visual styles. The underlying system was re-verified genuinely functional with a real two-browser-tab test (not just cosmetic): a teacher tab created a room, a student tab joined with a room code pre-filled from the join link, the teacher's live roster updated in real time with the student's name/grade/status, and Start/Pause/Lock/Remove all worked — zero errors on either page. One honest caveat carried over from the original design (not something this pass changed): inside an active classroom session, the student's progress bar and the analytics that come from it are driven by a deterministic practice simulation rather than the student answering real live SAT/ACT/TSIA2 items from the question bank — the room/roster/session-control layer is real, the per-question content inside a running classroom session is a simulation. Wiring the actual exam engine into a classroom session is a larger project; see the roadmap below.

## Fourth pass: flashcards organized by exam instead of one mixed list

Feedback on the flashcard feature after the third pass: it worked, but the 12 cards mixed SAT, ACT, and TSIA2 tips in one unlabeled sequence, so pressing Next jumped between unrelated exams with no way to pick just one. Rebuilt the deck as 30 tagged cards (10 SAT, 10 ACT, 10 TSIA2, covering the major skill areas of each exam — grammar/transitions, algebra, geometry, evidence-based reading, and science/data reasoning where each exam actually tests it) with All/SAT/ACT/TSIA2 filter tabs at the top of the modal. Picking a tab filters the deck to that exam only and resets to the first card, so Next/Prev never crosses into another exam's cards; each card also shows its subject as a small tag. Verified with Playwright: the All tab shows all 30, each subject tab shows exactly its 10, cycling through with Next never leaks a card from a different subject, and flipping still shows the matching answer for whichever card is showing.

## Fifth pass: flashcard deck expanded to 25 cards per exam

Feedback after the fourth pass: 10 cards per subject was too few for real studying. Expanded to 25 cards per exam (75 total, still zero topic repeats within a subject) covering skill areas the first 10 didn't reach — SAT cross-text connections, rhetorical synthesis, quadratics (factoring and the formula), triangles/Pythagorean theorem, scatterplots; ACT semicolons/colons, apostrophes, modifier placement, logarithms, comparing viewpoints; TSIA2 mean, fractions/decimals, area & perimeter, volume, comma usage. Verified with Playwright: All=75, each subject tab filters to exactly 25, and Next/Prev still never crosses into another subject's cards.

## Sixth pass: flashcards rewritten as real practice questions, expanded to 100

Feedback after the fifth pass: the cards should feel like actual SAT/ACT/TSIA2 practice questions — close to what really shows up on those exams — not abstract rule-of-thumb reminders, and the total should be 100. Rewrote every card (34 SAT, 33 ACT, 33 TSIA2, still zero repeats within a subject) as an original, self-contained question with a worked answer: grammar/editing items present an actual sentence and a choice to make; math items are solvable problems with the answer shown step by step; reading/rhetoric items embed a short excerpt or scenario and ask the same kind of question the real exam asks (main idea, command of evidence, tone, data interpretation, cross-text comparison, and so on). To be clear about originality: nothing here reproduces an actual copyrighted College Board, ACT Inc., or TSIA2 test question — every item is newly written, matching the style, format, and difficulty of each exam's real question types, the same approach already used for this site's full question bank. Verified with Playwright: total is exactly 100 (34/33/33), every card renders without a JS error, cycling through an entire subject's deck never leaks a card from another exam, and even the longest multi-sentence question wraps cleanly inside the card.

## Seventh pass: flashcard split rebalanced to match real exam proportions

The user asked what the "normal" question split should be instead of an even 33/33/34 across SAT/ACT/TSIA2. Researched the actual current question counts on each real exam: Digital SAT is 98 total items (54 Reading & Writing + 44 Math); the current "Enhanced ACT" format (rolled out 2025) is 131 core items (English 50 + Math 45 + Reading 36, not counting the optional Science section); TSIA2's initial, non-adaptive screening is 50 items (20 Math + 30 ELAR). Scaling that real ratio (98 : 131 : 50) down to a 100-card deck rounds to 35 : 47 : 18 — ACT genuinely has the most questions of the three real exams, so it now has the largest flashcard share, and TSIA2's short initial screening gives it the smallest. Added 14 new ACT cards (semicolons, intro-phrase commas, nonessential "which" clauses, vague pronouns, dashes, less/fewer, matrices, absolute value, successive discounts, cylinder volume, claim-then-evidence structure, correlation vs. causation, multi-variable data, tone from word choice) and 1 new SAT card, and trimmed TSIA2 to 18 by keeping one card per official TSIA2 math domain plus its 11 strongest ELAR cards. Worth noting: this same real-exam-proportion data was already being used elsewhere on the site — the "Full Adaptive/Core Simulation" exam engines already use the official 27/27/22/22 (SAT) and 50/45/36 (ACT) per-module counts, so this flashcard rebalance now matches rather than contradicts the rest of the site.

## Eighth pass: TSIA2 Math/English targets, and a real live-update bug fixed along the way

The user asked for a TSIA2 target next to the existing SAT/ACT targets in the "Score goal" card, split into English and Math — unlike SAT (400-1600) or ACT (1-36), TSIA2 doesn't have one composite score; it's college-readiness pass/fail per domain. Researched the real current numbers: TSIA2 Math and ELAR are both scored on a 910-990 scale, with statewide college-ready benchmarks of Math >=950 and ELAR >=945 (plus an essay score >=5), set uniformly by Texas Administrative Code §4.57 — no Texas college can require a different readiness cutoff. Added two new inputs defaulting to those real benchmarks, saved alongside the SAT/ACT targets.

Deliberately did not fabricate a predicted TSIA2 scaled score the way SAT/ACT already get one — the app has an existing, honest design decision (see `renderTsiEstimate`) that TSIA2's adaptive scoring can't be concorded to a fake precise number, only a readiness label. The new goal-card line follows the same rule: recent practice accuracy plus the existing readiness label, next to the saved targets, never an invented 910-990 estimate.

Testing this surfaced a real, pre-existing bug worth fixing while in the same code: the goal card only ever rendered once, 300ms after page load, and never refreshed after a test actually finished in the same session — so completing a full SAT, ACT, or TSIA2 practice set wouldn't update the visible goal summary without a manual page reload. Added a live refresh call at the end of both test-finishing functions so all three now update immediately. Verified with Playwright: defaults render correctly, a changed TSIA2 target survives a reload, and a completed 16-question TSIA2 diagnostic updates the goal summary live in the same session — zero errors, full regression suite still clean.

## Ninth pass: site-wide reorder to TSIA2, SAT, ACT

The user asked to change the display order everywhere from SAT/ACT/TSIA2 to **TSIA2, SAT, ACT**, sent with a reference mockup and the instruction to make "absolutely everything" match it. Went through the whole repo and reordered every place the three exams are listed together:

- `index.html`: the nav dropdown, the hero exam cards, the footer links, the daily-practice buttons, the Score Goal card (fields and all supporting JS), the review-due buttons, all three `<select>` dropdowns (question bank, goal picker, teacher exam picker), the progress tabs, and the internal JS arrays/objects that loop over the three exams (cosmetic reordering only — none of it changes behavior, since these are hash-routed sections shown one at a time, not scrolled past in order).
- The 11 standalone pages (`sat.html`, `act.html`, `tsia2.html`, `about.html`, `resources.html`, `graduation-guide.html`, `contact.html`, `privacy.html`, `terms.html`, `disclaimer.html`, `editorial-standards.html`) plus `teachers.html`: top nav links, and on `teachers.html` the exam `<select>` and the "Choose TSIA2, SAT, or ACT" guide text.
- `sitemap.xml`: URL entry order.
- All SEO/metadata and body text that mentioned the three exams together — the page `<title>`, Open Graph and Twitter tags, JSON-LD descriptions, the homepage eyebrow line ("Free TSIA2, SAT & ACT Practice"), and prose across About/Terms/Disclaimer/Editorial Standards/Resources.

One deliberate judgment call on that last item: reordering the `<title>` tag and meta description to lead with "TSIA2" instead of "SAT"/"ACT" is the one change here with any real SEO angle, since SAT and ACT are the higher-search-volume terms nationally. In practice, keyword *order* inside a title or description has a negligible effect on ranking or click-through compared to keyword *presence* — all three terms are still there — so this was safe to change as asked. If organic search traffic for SAT/ACT terms ever needs closer attention, this specific tag is the one line worth revisiting first.

Also a deliberate scope decision: did not physically move the giant `sat-center`/`act-center`/`tsi-center` `<section>` blocks in the DOM. These sections are shown one at a time via hash routing and JS show/hide classes, not scrolled past in sequence, so their DOM order has no user-visible effect — reordering hundreds of lines of deeply nested markup there would carry real risk for zero visible benefit.

Verified with Playwright: hero card order, nav dropdown order (and that the mega-menu still opens on hover), footer links, goal card field order, daily/review buttons, all three selects, progress tabs, and cross-page nav order across all 12 secondary pages all read TSIA2 → SAT → ACT. Full regression suite (6 passes, 15 pages) still clean — the one flagged item (`net::ERR_TUNNEL_CONNECTION_FAILED` on the AdSense script) is this sandbox blocking an outbound ad-network call, unrelated to this change and present before it too.

## Recommended next steps for performance, brand, and quality

Not done in this pass, but worth doing before or shortly after launch, roughly in priority order:

1. **Split `index.html` into smaller loaded chunks or defer non-critical scripts.** The file is ~500KB of inline HTML/CSS/JS in one document — everything (SAT engine, ACT engine, TSIA2 engine, Teacher tools, the 5,000-item generated bank) loads and runs on every visit even if a student only ever touches one exam. Lazy-loading the practice engines per-exam, or at minimum moving script execution to `defer`, would measurably improve first paint on slower connections.
2. **Real per-page OG images**, not one shared image for every page — a SAT-specific vs. ACT-specific vs. Teacher-specific preview card reads better when a link is shared.
3. **Build the `skills/*.html` pages** (see Known gaps) — real content there is also real long-tail SEO surface area that's currently just internal dead links.
4. **A real, monitored contact address** in place of the `support@scorepathpractice.com` placeholder, and a `terms.html` reviewed by an actual lawyer given the site handles student data and classroom monitoring.
5. **Lighthouse / PageSpeed pass** once hosted on the real domain — the animation and gradient-heavy CSS is unlikely to cause problems, but it's worth checking Cumulative Layout Shift specifically around the sticky header and the scroll-reveal (`revealV5`) sections.
6. **A short "Brand" style guide** (exact hex values, logo usage, the "S" mark do's/don'ts) once this becomes a real, marketed product — right now the brand mark exists (favicon, PWA icons, OG image) but isn't documented anywhere for a second designer to pick up consistently.

## Known gaps (not fixed in this pass)

- **The question bank is significantly less duplicated than before, but not fully unique.** After the fix described above, roughly 48% of the 5,179 generated items are byte-for-byte distinct from every other item; the rest still repeat a smaller number of underlying templates (mostly grammar/usage items like "choose the sentence that is grammatically complete," which draw from a hand-written list of 8–14 example variants cycling across ~100–200 generated slots). Getting to genuinely ~5,000 distinct questions would mean either writing several hundred more hand-crafted example sentences/passages for the lowest-variety templates, or reducing the on-page "5,000 questions" claim to the honest current unique count. Either is a reasonable next step — this pass prioritized fixing the actual generator bug (which was producing outright duplicates, the more serious problem) over authoring net-new content.
- **`skills/*.html` pages.** `sat.html`, `act.html`, and `tsia2.html` each describe 6–8 individual skills (e.g. "SAT Algebra") in a plain descriptive card with no link — they used to link to `skills/sat-algebra.html`-style pages that were never built, and those dead links have now been removed rather than left to 404. Building the pages for real (not templated stubs) is the top recommended next step — see "Monetization roadmap" below.
- **`contact.html`'s form** needs a real Web3Forms access key pasted in before it can actually deliver messages — see "Turning on the contact form" below. Until then it correctly tells visitors it isn't connected yet, rather than pretending to work.
- **Teacher Classroom sessions run on simulated student progress, not the real question bank.** `student-room.html` genuinely joins a room, receives real session control (start/pause/lock/end) over BroadcastChannel/Firebase, and reports real progress/status back to the teacher — but the "practice" a student does inside a running session is a deterministic simulated progress bar and simulated per-question responses, not the student actually answering real SAT/ACT/TSIA2 items from the shared bank. The teacher-side roster, live progress, and analytics tabs are all real and correctly reflect whatever data comes in — they'd work identically with real responses. Wiring the actual exam engine into a classroom session (so a joined student answers real questions and the teacher's analytics reflect real accuracy/skill/error data) is the single most valuable next step for this feature, and a meaningfully sized one — it means sharing the question-rendering engine between `index.html` and `student-room.html` and syncing real answer events instead of the current simulation.
- **Firebase and Google AdSense are both still unconfigured by design.** See "Turning on cloud sync" and "Turning on ads" below — this project intentionally ships with honest placeholders instead of fabricated credentials.
- **`terms.html`** is a plain-language starting point, not a lawyer-reviewed document — the page says so.

## Turning on cloud sync (Firebase)

1. Create a Firebase project + Web App, enable Authentication (Email/Password + Anonymous) and Firestore (student sync) and/or Realtime Database (classroom sync).
2. Paste your Web App config into `firebase-config.js`.
3. Replace development database rules with production rules that limit teachers to rooms they own and students to rooms they joined (see `FIREBASE-SETUP.md`).
4. Test with two separate browsers/devices before launch. `ScorePathRealtime.isCloudSynced` and `ScorePathSync.configured` let you confirm which mode is active from the console.

## Turning on ads (Google AdSense)

Ad inventory now lives on 7 pages, in two places:

1. Get your site approved for [Google AdSense](https://adsense.google.com) (needs a live, real domain — see "Going live" below). Your audience (SAT/ACT/TSIA2 test-takers, roughly ages 14–19) does **not** need child-directed (COPPA) tagging — that only applies to content directed at users under 13 — and Google's teen ad-safety protections (restricted ad categories, no personalization for known under-18 accounts) are enforced automatically on Google's side; there's nothing to configure for that on the publisher side.
2. **`index.html`**: find `ADSENSE_CLIENT_ID` and `ADSENSE_AD_SLOTS` (search for `scorepath-adsense-consent`) and fill in your publisher ID and the slot IDs for the `hub`, `trust`, and `teacherTools` placements.
3. **`consent-banner.js`** (shared by `sat.html`, `act.html`, `tsia2.html`, `about.html`, `resources.html`, and `graduation-guide.html`): find `ADSENSE_CLIENT_ID` near the top of the file and fill in the same publisher ID as step 2 — one line, applies to all six pages at once. Each page's single `.spAdSlot` placement needs a `data-ad-slot-id="..."` attribute added to its `<div class="spAdSlot" data-ad-ready="true">` tag with the slot ID for that page (search `spAdSlot` in each file).
4. Replace `ads.txt` with the real snippet AdSense gives you.
5. Ads activate automatically for visitors who accept the cookie banner (on any of the 7 pages); nothing changes for visitors who decline, and ads never show on login/account pages, legal pages, or during an active practice test.

## Turning on the contact form (Web3Forms)

`contact.html` ships with a real, working contact form — it just isn't connected to an inbox yet, on purpose, so no fake submissions ever silently vanish and no fabricated address is ever shown.

1. Go to [web3forms.com](https://web3forms.com) and enter the email address you want messages delivered to. No account or password is needed — you just verify that inbox once.
2. Web3Forms emails you a public **access key** instantly.
3. Open `contact.html`, find the hidden input `<input type="hidden" name="access_key" value="YOUR_WEB3FORMS_ACCESS_KEY">`, and replace `YOUR_WEB3FORMS_ACCESS_KEY` with the key you were emailed.
4. That's it — submissions now arrive in your inbox as an email. The access key is safe to leave visible in the page source: it can only be used to *send* a form submission to your verified address, never to read your inbox or send arbitrary email, and it is not your email address itself.
5. Optional hardening once you have a real domain: in the Web3Forms dashboard you can restrict the key to only accept submissions from `scorepathpractice.com`, and add a reCAPTCHA/Turnstile key for extra spam protection (the built-in honeypot field already blocks basic bots).

Until step 3 is done, visitors who submit the form see an honest "This form is not connected yet" message instead of a fake success confirmation.

## Setting up Google Search Console

1. Add your property in [Search Console](https://search.google.com/search-console) and verify ownership (HTML file or meta tag method both work on a static host).
2. Submit `sitemap.xml`.
3. Build out the `skills/*.html` pages (see "Known gaps") if you want those specific skill queries to rank — right now they're only internal links, not indexed pages.

## Monetization roadmap

Everything below is a concrete, honest next step — no invented traffic numbers, no guaranteed revenue figures (nobody can promise those for a site with no live traffic history yet), just what actually moves the needle for an ad-supported education site, in priority order.

1. **Get AdSense and Search Console live first — nothing else matters until traffic exists.** Ad revenue is a function of (pageviews) × (ad units seen) × (click-through rate) × (CPC), and right now the first factor is zero because the site isn't live. Steps 1–4 of "Turning on ads" and "Setting up Google Search Console" above are the actual first priority.
2. **Build the 22 `skills/*.html` pages for real.** This is the single highest-leverage thing left undone. Each one targets a specific, real, lower-competition search query ("SAT algebra practice," "ACT English conventions practice," "TSIA2 quantitative reasoning practice") that the current 4 pages (home, sat, act, tsia2) don't individually rank for. More indexed pages targeting more specific queries is the most reliable lever for organic search traffic on a content site, and every one of those pages is now ad-eligible using the same `.spAdSlot` pattern added to `sat.html`/`act.html`/`tsia2.html` this pass. This is real content work (each page needs genuine skill-specific explanation and a couple of real sample questions, not a templated shell) — budget real time for it rather than generating 22 thin pages fast, since thin/templated content is exactly what AdSense and Google Search both penalize.
3. **Fix the ~500KB single-file `index.html` load.** Every visitor downloads and executes the entire SAT + ACT + TSIA2 + Teacher engine on every visit, even a mobile visitor who only wants a 3-question TSIA2 diagnostic. Slow mobile load hurts both organic ranking (Core Web Vitals is a ranking factor) and ad viewability (a visitor who bounces before the page finishes loading never sees an ad). Splitting the practice engines to load per-exam, or at minimum switching inline `<script>` execution to `defer`, is the next real performance/monetization project — see item 1 under "Recommended next steps" above, which already flagged this before the monetization angle made it more urgent.
4. **Consider a second monetization stream once there's real traffic to justify it** — for example a legitimate, disclosed affiliate program for SAT/ACT prep books (Amazon Associates or similar) linked from `resources.html`, or an optional donation/support link. Don't add either speculatively before there's traffic to make it worthwhile, and any affiliate relationship needs a real account and a real FTC-compliant disclosure — never a fabricated "as an Amazon Associate" line without an actual account behind it.
5. **Revisit ad density only after real AdSense data exists.** Right now there's exactly one ad slot per page — a conservative, policy-safe starting point. Once the account has real traffic and Ad Manager reporting, Google's own guidance (and simple A/B testing via AdSense's built-in experiments) is a better guide than guessing at a "more ads = more revenue" assumption up front, which frequently backfires via higher bounce rates.

## Going live checklist

- Every page uses `https://scorepathpractice.com` as its canonical/OG domain already — point your real DNS at this domain, or do a project-wide replace if you're using a different one.
- Paste a real Web3Forms access key into `contact.html` (see "Turning on the contact form" above) so messages actually reach you.
- Fill in Firebase and AdSense config as above.
- Add a `CNAME` file at the repo root if hosting on GitHub Pages with a custom domain.

## Running it locally

Just open `index.html` in a browser, or serve the folder with any static file server:

```bash
python3 -m http.server 8000
```

then visit `http://localhost:8000`.

## Disclaimer

ScorePath Practice is an independent educational practice resource. It is not affiliated with, endorsed by, or sponsored by the College Board, ACT, Inc., or the Texas Higher Education Coordinating Board. All practice questions are original. Practice results are for learning purposes only and are not official test scores. See `disclaimer.html` for the full statement.
